project.name = mxu_publish
server.index = 127.0.0.1:8383
server.search = 127.0.0.1:8384

[id]
type = id
tokenizer = full

[order_id]
type = numeric
tokenizer = full

[site_id]
tokenizer = full

[column_id]
tokenizer = full

[origin_id]
tokenizer = full

[type]
tokenizer = full

[title]
type = title

[title_attribute]

[index_pic]
tokenizer = full

[author]
index = both

[brief]
index = both

[keywords]
index = both

[subtitle]
index = both

[source]
index = both

[source_link]
index = both

[publish_time]
index = both

[content]
type = body
